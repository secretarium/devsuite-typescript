import React from 'react';
import { Text } from 'react-native';
import Container from '../Container';

const Root: React.FC = () => {
    return (
        <Container>
            <Text>Home</Text>
        </Container>
    );
};

export default Root;