# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build pda-back`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t pda-back`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system pda-back && \
    adduser --system -G pda-back pda-back

COPY dist/apps/pda-back pda-back
RUN chown -R pda-back:pda-back .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix pda-back --omit=dev -f install

CMD [ "node", "pda-back" ]
