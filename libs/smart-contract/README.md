# smart-contract

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build smart-contract` to build the library.

## Running unit tests

Run `nx test smart-contract` to execute the unit tests via [Jest](https://jestjs.io).
